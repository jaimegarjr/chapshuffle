# Copy to .env (gitignored) and fill in real values.
# Loaded automatically by just (dotenv-load) for recipes like `just build-prod`.
# GA4 Measurement Protocol credentials — missing values disable telemetry in the build.
GA_MEASUREMENT_ID=
GA_API_SECRET=
